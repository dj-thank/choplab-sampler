# PR #64 Desktop autosave review receipt

This revision-bound receipt covers the focused Desktop startup-recovery, source-device, and close-time autosave repairs. It does not change the archive schema, three-generation policy, Android/iOS behavior, release artifacts, or provider claims.

- Current product/test repair: `28e97abcea28c5ed80eb04cca81b65c7e035715e`
- Current product tree: `17bd8cd2a1ca8fed6cc832a9dd7ab4f26e3ab737`
- Atomic playback-availability repair: `a2d897343d170cb31900616d30f2eaf95db770fe`, tree `66a979489562821af9a72e212762eba0bbd734ef`
- Pending-hydration repair: `bcc0c42ded0d87cd94b01c26255d5583fed39666`, tree `5c3e6ac0a9889042ea413d367d0a85353a6a85eb`
- Latest-main product/test integration: `7261a67c24bd30f95bc015e4b8b003d7a3079297`, tree `380258e09e597f24b3ad790bf59e445546ba08af`
- Review repair product/test commit: `82d21a4a8dd353d8cd7f268410987a7fab2423ca`, tree `d93cbf53c02dd7ba6ed21a4044b97c8321b53071`
- Hosted-oracle correction: `815d4c3fffab5deca3f0004c828929642993e492`, tree `e466b79a589da0a63d8e2e2314e982346f52a2b4`
- Runtime repair commit: `74f79524a35880398960af1db82597e33de68198`, tree `a2ca8344ee9b4fd773dcedf6fc82edbe00aaf24a`
- Latest-main integration parents: current product repair `28e97abcea28c5ed80eb04cca81b65c7e035715e` plus exact merged `main@4f56a693b86540d1be13a6eb2153a3cf96ef9396`, tree `25fa5505d39c9a3bb93481ff5060f0953e92d1fc`.
- Main preservation: #75 Android microphone cancellation source/tests/shared recording policy and its feature/current-snapshot/validation/plan updates are inherited unchanged from exact `main@4f56a69`. The integration tree is based directly on its exact tree and overlays only the five disjoint PR-owned Desktop/receipt paths.
- Main preservation: #74 reverse one-shot PCM source/tests/Desktop no-device regression and current snapshot/validation/plan files remain inherited through exact latest main.
- Main preservation: #73 recorder source/test blobs remain exact `3dec52b4598e3222503cbdc3abeade543e22e046` and `eea213f8716d92fe63f3f13d8180da94b3399657`. #74 legitimately advanced the shared hardening-plan blob to `df793a73854d08f2f670687c9b780500327ebc98`, which is inherited unchanged from exact `main@029500a`.
- Repair: recovered source hydration reserves its device-load revision before state publication, uses the persisted pitch, and opens the device outside the recovery monitor. A newer pitch/source operation revokes queued hydration, close revokes admitted hydration without waiting for device open, and deferred player teardown runs after any in-flight load.
- Repair: recovery clears prior device readiness before publishing state, so a synchronous pitch edit can establish newer readiness without the publication tail erasing it; the immediate-pitch regression also requires source playback to reach the engine.
- Repair: pending recovered-audio hydration is distinct from device failure. A play request during device open reports preparation, successful hydration restores the prior recovery status, and a later play reaches the engine; a newer source load preserves the same restoration owner.
- Repair: source playback availability is now one lock-protected `Unavailable`/`Pending`/`Ready`/`Failed` state. Play admission and hydration publication inspect or replace that state atomically, so play cannot combine readiness, pending, and failure fields from different device-load moments.
- Repair: NEW PROJECT invalidates the source-load epoch in addition to recovery ownership. A deterministic admitted-worker regression holds recovered hydration after its recovery check, resets, releases it, then uses a queued save as the executor barrier and requires zero discarded-source device loads.
- Repair: a failed master-pitch reload stops retained source audio and publishes a non-playing error state. Explicit `.wav`/`.choplab` startup keeps the existing autosave untouched while the controller still owns only its revision-0 placeholder; successful replacement or a real edit restores normal persistence.
- Focused regressions: reset revoking admitted recovered hydration; source play during pending recovered hydration; immediate recovered-state pitch edit; close during a blocked recovered-device load; failed pitch reload while playing; corrupt explicit startup preserving byte-identical autosave; plus the earlier save/export, failed replacement, stale recovery failure, device error, unchanged archive, and teardown-flush cases.
- Hosted correction: Windows compiled and reached 104 tests on `73b28b8`; only the new close regression raced between its completion latch and the thread's final termination. The corrected oracle joins the already-completed thread before asserting it is no longer alive; the runtime one-second close deadline is unchanged.
- Hosted exact-main evidence: head `95b5d43` passed Android `32737877285`, Windows `32737877323`, iOS `32737877440`, and supply-chain `32737877336`; the current pending-hydration repair requires a fresh exact-head run.
- Gates before publishing this repair: Python policy 39/39 and the pre-integration local public-surface scan of 396 candidates pass; the previous exact head `af87e8e` passed the hosted 397-candidate scan plus all four workflows. Conflict-marker scan and `git diff --check` pass. Gradle 9.7.1 remains unavailable locally because its wrapper cache is not writable and the distribution is uncached, so fresh hosted Windows compilation/tests/package and exact-head review are required.
- Documentation boundary: the current-snapshot promotion requested in review remains pending explicit authorization after the GitHub connector rejected writing `docs/PROJECT_STATE.md` because it contains extensive environment/artifact metadata. Exact latest-main blob `21c1cefe16402b94b4b3ebb1341fd8c8efda524a` is inherited without modification; this receipt does not claim that the separate promotion is complete.
