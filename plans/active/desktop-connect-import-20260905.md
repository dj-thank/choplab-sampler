# Desktop connect-to-import navigation

Root sole writer at 1c3827b in the dedicated refinement worktree. User asks the Desktop 連携 menu and Connect panel to provide a clear, simple path into internal audio import. Scope: desktop menu routing, panel presentation, actual source-hub handoff, Windows packaging and required local validation. Preserve source library, productions, provider session and running app. No new OAuth/provider mutation or device work.

- [ ] Route menu choices directly to Library, YouTube or Spotify source hub.
- [ ] Replace technical setup-first Connect panel with explicit import choices and secondary playback controls.
- [ ] Confirm panel click -> source hub -> login/import action, responsive layout and package.

Use a new Windows app-image directory; never overwrite the running app. Rollback only this task's scoped diff. Local UI/build evidence is not a new physical/provider gate.
